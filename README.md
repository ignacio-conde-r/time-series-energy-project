## Data

The original raw dataset `LD2011_2014.txt` is not included in this repository because it exceeds GitHub's 100 MB file size limit. The dataset can be downloaded from the original source and placed manually under the `data/` folder.
